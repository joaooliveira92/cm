# Research: retro-match-screen

Findings from `research` tickets on the `.scratch/retro-match-screen` map land here, one file per
ticket (`NN-<slug>.md`), linked from the ticket body and the map's Decisions-so-far.

Context pointer convention: each ticket's `## Answer` links its findings file; the map links the
ticket, never the findings file directly.