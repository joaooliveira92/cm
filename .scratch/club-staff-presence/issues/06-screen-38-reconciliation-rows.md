# 06: Screen 38's rows in a new group C ledger

Type: task
Blocked by: 04
Status: open

## Question

Group C has no `RECONCILIATION.md`. Only groups A and B have been reconciled, each as its own
dedicated effort. This effort ships one screen from group C and owes that screen's rows — not the
other fifteen.

Do the work:

- **Create the group C ledger** — a `RECONCILIATION.md` alongside the screens in
  `docs/specs/group_c_club_information/` — with a preamble stating plainly
  that it covers screen 38 alone and that the remaining fifteen screens are unreconciled, so a later
  reader does not read silence as "nothing to reconcile".
- **Classify screen 38's sections** against what shipped, using the vocabulary groups A and B
  already use. Most of it is `out-of-scope` against decisions this repo made long before this
  effort: staff contracts, wages, hiring, firing, vacancies, workload, responsibilities, staff
  search, and the permission and knowledge model all have no referent here.
- **Record what survives and in what form** — the read-only department-grouped list — and what was
  added that the import never asked for, since the import has no President and no Executive person
  at all.
- **Do not edit the import file.** The ledger's own preamble rule: import files are never edited,
  because their value is that you can always see what arrived.

Blocked on the page design because a ledger row describing what shipped needs the thing to have
shipped in design, not just in principle.
