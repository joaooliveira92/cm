Type: task
Status: ready-for-agent
Blocked by: None (can start immediately)

## Work

A spec section documenting the three wave-2 seed scenarios (`seedWithFreeAgent`, `seedWithIncomingBid`, `seedWithCounteredBid`) — their SQL mutations, UI guarantees, and which tests consume each. Extends the seed-save helper table from the wave 1 spec, adding a new "Wave 2" row group.

## Decisions

(none — the seed design was a pure grilling decision with no Agent Note promotion)

- [ ] Spec includes table of 3 seeds with build steps, UI guarantees, and consumer tests
- [ ] Section follows wave 1 spec tone and level of detail (seeds table format, `fresh` reference)
- [ ] All seed names match the wave 1 naming convention (`seedWithFreeAgent`, etc.)