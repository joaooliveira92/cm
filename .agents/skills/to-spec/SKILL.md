---
name: to-spec
description: "Turn recorded wayfinder decisions into a written spec. Alias for the cm-to-spec skill."
---

# to-spec

This is an alias. The real skill is [`.agents/skills/cm-to-spec/SKILL.md`](../cm-to-spec/SKILL.md) --
read that file and follow it.

The `cm-to-spec` original sets `disable-model-invocation: true` so that it is reached
deliberately through this name rather than picked up automatically. Do not duplicate its
instructions here; this file exists only to provide the short name and to keep the
skill listing's description accurate.
