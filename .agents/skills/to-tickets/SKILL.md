---
name: to-tickets
description: "Break a spec into sequenced implementation tickets under .scratch/<feature>/. Alias for the cm-to-tickets skill."
---

# to-tickets

This is an alias. The real skill is [`.agents/skills/cm-to-tickets/SKILL.md`](../cm-to-tickets/SKILL.md) --
read that file and follow it.

The `cm-to-tickets` original sets `disable-model-invocation: true` so that it is reached
deliberately through this name rather than picked up automatically. Do not duplicate its
instructions here; this file exists only to provide the short name and to keep the
skill listing's description accurate.
