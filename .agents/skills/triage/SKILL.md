---
name: triage
description: "Triage an issue in the .scratch/ tracker and apply the repo's triage labels. Alias for the cm-triage skill."
---

# triage

This is an alias. The real skill is [`.agents/skills/cm-triage/SKILL.md`](../cm-triage/SKILL.md) --
read that file and follow it.

The `cm-triage` original sets `disable-model-invocation: true` so that it is reached
deliberately through this name rather than picked up automatically. Do not duplicate its
instructions here; this file exists only to provide the short name and to keep the
skill listing's description accurate.
