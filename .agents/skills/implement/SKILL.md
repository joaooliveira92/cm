---
name: implement
description: "Implement a ticket or spec, promoting linked proposed Agent Notes to implemented in the same commit. Alias for the cm-implement skill."
---

# implement

This is an alias. The real skill is [`.agents/skills/cm-implement/SKILL.md`](../cm-implement/SKILL.md) --
read that file and follow it.

The `cm-implement` original sets `disable-model-invocation: true` so that it is reached
deliberately through this name rather than picked up automatically. Do not duplicate its
instructions here; this file exists only to provide the short name and to keep the
skill listing's description accurate.
