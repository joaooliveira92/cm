# Group B reconciliation ledger

The specs in this directory are an **import**, not a set of requirements. They read as generated from a
generic template rather than authored against this game, and they routinely describe subsystems this
project has never decided to build. This ledger records, per spec section, every place the import is
knowingly not followed, and why.

It is an index, not a store. A row states the divergence in one line and points at the decision that
carries it. Anything needing more than a line belongs in an Agent Note under
[.agents/notes/](../../../.agents/notes/), with the row's Anchor linking to it.

The import files are never edited. Their value is that you can always see what arrived.

The format is the one the [Group A ledger](../group_a_application_shell_and_game_lifecycle_remaining/RECONCILIATION.md)
pilots; that file is the fuller worked example. Group B adopts it screen by screen as each is audited,
and, as in Group A, a blanket scope trim ran across the whole group before any screen was audited. So a
screen can carry rows without anyone having read it; the status line is what says which.

## How to read a row

| Field | Meaning |
|---|---|
| Sections | The cited import sections: file, `§N`, and the heading text. Heading text is carried because this import's numbering is not trusted to be stable. |
| Kind | One of the four below. |
| What the spec asks | One line, so the ledger is readable without opening the import. |
| Disposition | What this project does instead, or nothing. |
| Anchor | Meaning set by Kind — the mandatory field that stops an entry being an unsupported assertion. |

### Kinds

| Kind | Meaning | Anchor holds | Can it return? |
|---|---|---|---|
| `out-of-scope` | Ruled permanently outside this game. | The reason. | No. |
| `contradicted` | This codebase already made an incompatible decision. | The Agent Note or `CONTEXT.md` term carrying that decision. | Only if that decision is overturned. |
| `deferred` | Wanted, in scope, not built. | The owning spec group, or `unscheduled`. | Yes. |
| `renamed` | The concept exists here under different vocabulary; behaviour agrees. | The `CONTEXT.md` term. | N/A — this row asserts agreement. |

Sections followed as written get **no row**. What that silence means depends on the screen's status
line:

| Status | What silence asserts |
|---|---|
| `Audited` | Everything not listed below is followed. A section-by-section pass was made. |
| `Reviewed` | Nothing. The rows are the material conflicts a single-session pass found; unlisted sections were not individually checked. |
| `Not yet audited` | Nothing. |
| `Disposed in full` | Nothing is left silent. The screen was ruled out of scope as a whole file, and its one row below disposes of every section at once. No section-by-section pass was made and none is owed. |

Those four are the whole vocabulary. Every screen in the Coverage table carries exactly one of them,
and a screen moves between them only by a ticket that says so.

A screen carrying rows is therefore not the same as an audited screen. The **blanket sweeps**
(group-b-blanket-disposals, 2026-09-07) applied rulings settled at charting or inherited from Group A
across every surviving screen at once, before any of them was read. Those rows narrow what an audit has
to cover; they do not make the screen audited, and its status stays `Not yet audited` until a session
reads it.

## Coverage

| Screen | Import file | Status |
|---|---|---|
| 22 Global Application Shell | [22_global_application_shell.md](22_global_application_shell.md) | Reviewed |
| 23 Continue and Advance Time | [23_continue_and_advance_time.md](23_continue_and_advance_time.md) | Reviewed |
| 24 News Inbox | [24_news_inbox.md](24_news_inbox.md) | Not yet audited |
| 25 Individual News Message | [25_individual_news_message.md](25_individual_news_message.md) | Not yet audited |
| 26 News Filters | [26_news_filters.md](26_news_filters.md) | Not yet audited |
| 27 Background Processing and Updating Game | [27_background_processing_and_updating_game.md](27_background_processing_and_updating_game.md) | Audited |
| 28 Calendar and Schedule | [28_calendar_and_schedule.md](28_calendar_and_schedule.md) | Reviewed |
| 29 Manager Notebook | [29_manager_notebook.md](29_manager_notebook.md) | Disposed in full |
| 30 Manager History | [30_manager_history.md](30_manager_history.md) | Reviewed |
| 31 Manager Profile | [31_manager_profile.md](31_manager_profile.md) | Reviewed |
| 32 Manager Chat and Multiplayer Communication | [32_manager_chat_and_multiplayer_communication.md](32_manager_chat_and_multiplayer_communication.md) | Disposed in full |

## The multiplayer axis

Group A removed the multiplayer, network, cloud and multi-manager axis wholesale. Every Group B import
file carries that axis anyway, and mostly in identical words, because the files share a template: all
nine surviving screens have the same `## 10. Permissions and multiplayer behavior`, the same
manager-scoped navigation-history bullet in §9, and the same §17 persistence sentence. The rows on each
screen below dispose of it screen by screen, and their repetition is the import's, not the ledger's.

Three phrases carry the axis in disguise and account for most of its spread outside §10. Each is
disposed of once here so a screen audit can cite the disposal instead of deriving it again.

### The active manager

The import treats "the active manager" as a scope: an identity that decides what private data may be
displayed, whose drafts and navigation history are kept apart from another's, and whose control is
handed over at a switch. There is no scope, because there is nothing to scope against — [`CONTEXT.md`](../../../CONTEXT.md)'s
**Save** entry fixes exactly one human manager per Save. Every occurrence resolves to *the* manager, so
a query scoped to them is the unscoped query, and a clause about protecting one manager's data from
another disposes of itself. Nothing switches, so nothing is cleared at a switch. AI-managed clubs are
simulated, not viewers.

### The career revision

The import carries a monotonic `revision` on requests and responses so a client can detect that
somebody else has written since it read, discard stale responses, and refuse to act on a superseded
view. There is no somebody else. Persistence is one local SQLite file with a single writer, and it is
durable at commit — see [Durable at commit](../../../.agents/notes/proposed/architecture/2026-08-30-durable-at-commit-persistence.md)
and [Domain-bounded Deciders](../../../.agents/notes/implemented/architecture/2026-08-27-domain-bounded-deciders-and-chunked-resimulation.md).
With no concurrent writer, a revision has nothing to detect and a conflict has nothing to be between.
Screen 23's rows dispose of the same model where it appears there.

### The permission context

Permissions in the import partition one career among participants holding different rights: a trusted
layer decides them, a client may not grant them, and the UI degrades to `permission_denied` when they
are missing. One participant holds every right, so there is no partition to enforce, no denial to
render, and no permission decision that can go stale. `permission-denied` as a *state* is therefore not
a state this application has.

These three, plus the network clauses they travel with — `offline`, `conflicted`, the authoritative
host or server, remote refresh, host disconnect and migration — are what the per-screen rows below
dispose of. Their anchor is this section, which rests on [`CONTEXT.md`](../../../CONTEXT.md)'s **Save** entry and on the Group A
removal recorded in the [Group A ledger](../group_a_application_shell_and_game_lifecycle_remaining/RECONCILIATION.md).

## Screen 22: Global Application Shell

Status: **Reviewed** (group-b-reconciliation ticket 01, 2026-09-07).

The import's "Global Application Shell" is not the shell Group A audited. That one was the pre-career
boot shell — Main Menu, then Load Career. This one is the chrome every career route renders inside:
`apps/desktop/src/renderer/chrome/CareerChrome.tsx`, its `header/` and `bottom-bar/` directories, the
two Continue bands, and the `navigation/` modules they drive — around 700 lines across nine files, and
the only surface every Group B screen shares.

`Reviewed` rather than `Audited`, and the line is worth drawing precisely. All 23 sections were read
and each is either on a row below, on one of the three blanket rows, or followed. What was not done is
the behavioural half: §12's announcement and colour-alone rules, and §20 criterion 5's promise that an
unread count updates without resetting focus, are claims about what happens at runtime, and confirming
them means driving the chrome rather than reading it. Those are the sections a later `Audited` pass
would have to settle.

| Sections | Kind | What the spec asks | Disposition | Anchor |
|---|---|---|---|---|
| §4 Conceptual layout (the bottom status bar), §2 Primary user goals (monitor save, processing and network states), §20 criterion 4 | `contradicted` | A bottom status bar carrying context status, save status, network status and notifications, with Continue, save, processing and network states kept visibly distinct. | The career chrome has no bottom bar at all. `ShellBottomBar` exists but belongs to the creation flow's footer and is never mounted here. Two of the four have referents and both are reported elsewhere: processing on the control that causes it, which reads `Advancing…` while an advance is in flight, and in the outstanding and result bands below the navbar; notifications as an unread count badge on the News destination. | Save has no state to report — [`CONTEXT.md`](../../../CONTEXT.md)'s **Save** is durable at commit, so there is no dirty, saving or unsaved-changes condition for a status area to show — and network is the axis disposed above. [Career chrome and date/Continue bar](../../../.agents/notes/implemented/architecture/2026-08-31-career-chrome-and-date-continue-bar.md) is the layout this chrome descends from, and it puts the career's report beside the career's control rather than in a separate status region. |
| §4 Conceptual layout (Breadcrumbs) | `contradicted` | A breadcrumb row: `Club > Squad > Player`. | There are none. The navbar's centre zone holds seven sections and reveals a context submenu strip for the active one; where the player is showing is which section and which item are highlighted. | The navigation model is two levels deep — section, then item, in `apps/desktop/src/renderer/navigation/nav-config.ts` — so a trail would carry at most two entries, one of them the section already highlighted beside it. There is no third level for it to earn, because no career route names an entity. |
| §2 Primary user goals, §4 Conceptual layout (the primary row), §7 Principal interactions (activate a primary destination) | `deferred` | Primary destinations for squad, staff, tactics, training, fixtures, finances, transfers, scouting, competitions, news and search. | Seven sections ship: Squad, Tactics, Training, Recruitment, Analysis, News and Club. Most of the import's list is present under a different grouping — fixtures, competitions and the season live inside Analysis, transfers inside Recruitment. Three have no destination and no route: staff, finances and scouting. Training has its navbar slot but no screen, so its default destination falls back to `squad`. | `unscheduled` for finances and scouting. Training needs only its screen — the slot is built and the fallback is deliberate, so that the navbar never offers a dead link. Whether staff earns a slot is not this screen's decision. |
| §9 Navigation behavior (the *prior safe screen*), §3 Position in the navigation model, §20 criterion 3, §21 Recommended tests (Back and Forward history) | `contradicted` | Back and Forward move through **safe** navigation history, and history restores only safe, valid routes. | Back and Forward are the router's own history stack and nothing else: `navigateBack` and `navigateForward` call `history.back()` and `history.forward()` with no filtering, no validation, and no notion of a route being unsafe. `canNavigateBack()` disables Back at the bottom of the stack; the router exposes no `canGoForward`, so Forward stays enabled and is a no-op at the top, which is the contract a browser's forward button has. | **This is the disposal of "prior safe screen" that the rest of Group B cites.** Safety filtering presupposes that a route in the stack can go stale. None can: every career destination in `apps/desktop/src/renderer/navigation/destinations.ts` is `/career/$saveId/<section>`, parameterised by the Save and nothing else, and the Save is loaded for as long as the chrome is mounted. So the previous entry is always valid, and "the prior safe screen" means "the previous screen". |
| §9 Navigation behavior (deleted or inaccessible entities), §19 Edge cases (the selected entity is deleted or becomes inaccessible; a late response arrives after navigation away), §20 criterion 7, §21 Recommended tests (Deleted-entity fallback) | `contradicted` | A safe unavailable state when a linked entity is deleted or becomes inaccessible, a shell that survives it, and a test for the fallback. | There is no case to handle. No route in this navigation model carries an entity id, so nothing in the history stack can dangle, and there is no entity-detail screen for a deleted row to strand the player on. | The route model above: section-scoped destinations parameterised only by the Save. This row disposes of the *shell's* handling; a future screen that routes to a player or a club reopens the question for that screen, not for the chrome. |
| §2 Primary user goals (open recent entities), §4 Conceptual layout (Search in the primary row), §7 Principal interactions (open global search and recent entities) | `contradicted` | A global search over game data, reachable from the header, and a recent-entities list that opens without losing the current context. | `HeaderSearch.tsx` sits exactly where the import puts search, and does something else: it renders the `open-palette` Action record and opens the command palette. The only searchable surface in the application is the Action registry — its ids and labels — not players, clubs or competitions. There is no recent-entities list anywhere in the chrome. | [Action registry](../../../.agents/notes/implemented/architecture/2026-08-29-action-model.md) — the palette searches Actions, and the header button, the palette and the help overlay are three views of one record so they cannot drift. Game-data search has no model behind it: there is no cross-entity read to search. Recents are `unscheduled`. |
| §6 Core data model (`GlobalShellState`) | `contradicted` | One shell state object carrying `careerId`, `managerId`, `organizationId`, `careerDate`, `primaryDestinations`, `history`, `unreadCounts`, `processingState`, `saveState`, `networkState` and `revision`, serializable and immutable per revision. | No such aggregate exists, and the difference is structural rather than cosmetic. The chrome composes five independent reads — `managerProfileAtom`, `leagueTableAtom`, `saveSummaryAtom`, `tacticsAtom`, `newsInboxAtom` — plus the `phase`/`advancing` scope state it publishes itself for the Action registry's availability predicate. `primaryDestinations` is not data at all: `NAV_SECTIONS` is a module constant, so the navbar's shape is fixed at compile time and cannot arrive late, arrive empty, or disagree with the router. | [Career chrome and date/Continue bar](../../../.agents/notes/implemented/architecture/2026-08-31-career-chrome-and-date-continue-bar.md). Each read fails independently, which is why the header renders a per-metric placeholder rather than one loading state for the whole frame, and why a compile-time navbar is worth having: the reachability invariant over `NAV_SECTIONS` is assertable in a unit test. |
| §8 Screen and operation states | `contradicted` | Eight shell-level states: `loading_context`, `ready`, `navigating`, `processing`, `saving`, `offline`, `permission_changed`, `recovery_required`. | The chrome has no state machine and no shell state. Each read carries its own `_tag`, the advance mutation carries `waiting`, and what renders is the union of those. Four of the eight have no referent here: navigation is synchronous, so nothing is `navigating`; the Save is durable at commit, so nothing is `saving`; and `offline` and `permission_changed` belong to the disposed axis. `recovery_required` names a recovery flow this application does not have. | [`CONTEXT.md`](../../../CONTEXT.md)'s **Save** for durability, and the per-read composition above. A shell-wide state would have to be computed from five independent reads that legitimately disagree — one loaded, one in flight, one failed — which is the state the per-metric placeholder exists to render honestly. |
| §11 Validation and error handling (preserve the last valid view; corrective actions) | `deferred` | Keep the last valid view when a refresh fails, and offer corrective actions — Retry, Clear Filters, Return, Open Recovery. | Neither, and this is a real gap rather than an absent referent. A failed read in the chrome collapses to `null` and the header renders its `NO_VALUE` placeholder with a `title` reading that the metric "is not available yet" — the same thing it shows while the read is still in flight, so a failure is indistinguishable from a load. There is no Retry anywhere in the frame. The one path that does report properly is a failed *advance*: `describeRpcError` puts the message in the result band. | `unscheduled`. The advance path shows the shape the rest should take; nothing has decided whether the header's reads deserve it. |
| §12 Accessibility requirements (restore focus after dialogs, drawers and linked detail screens close) | `deferred` | Focus restored when a dialog, a drawer, or a linked detail screen closes. | Route-level restoration is built but stops one level short of what it claims. `navigateBack` sets the `__back__` marker and the arriving `RouteView` focuses that screen's wrapper `div` — the screen, not the region or the control the player left from. Nothing records a per-screen main region, so `requestBackFocus`'s documented contract, "restore the arriving screen's remembered main region", is not what happens; `BACK_RESTORE_MARKER` is read in exactly one place and always resolves to the screen id. | [Intra-screen focus model](../../../.agents/notes/proposed/architecture/2026-08-29-intra-screen-focus-model.md) owns the region and item level and is still `proposed`. The screen-level half shipped; the remembered-region half did not, which is why the code's comments describe a contract the code does not keep. |
| §12 Accessibility requirements (reduced motion, high contrast, 200 percent text scaling, right-to-left layout), §13 Localization requirements, §14 Responsive behavior, §21 Recommended tests (200 percent scaling, Right-to-left layout) | `deferred` | Localized labels, dates, numbers and plural forms with stable ids kept separate from display names; wide, standard, narrow and ultrawide layouts; reduced motion, high contrast, text scaling to 200 percent, and right-to-left layout. | None of it. Every string in the chrome is a hard-coded English literal, the window is a fixed 1200×800, and the navbar's overflow strategy is horizontal scroll at one width. | `unscheduled` — the same row Group A Screen 1 and Screen 23 carry. Localization and display options have obvious referents and are not an auditor's to rule out; Group A Screen 17 owns resizing. |
| §15 Performance requirements (virtualize long lists, debounce search and filters, stable row identity and scroll position, rate-limit live updates) | `out-of-scope` | Virtualized long lists, debounced search and expensive filters, preserved row identity and scroll position, rate-limited live updates and progress events. | There is no list on this surface. The chrome renders a navbar, a four-metric row, and at most two bands of a few rows each. §15's remaining clause — keep heavy queries and transformations outside the renderer — is followed and needs no row: every value in the frame arrives by RPC from the main process. | Template scaffolding: §15's list requirements are repeated verbatim across the group's screens, and on a frame that renders no collection they have nothing to apply to. Screen 23 carries the same row for the same reason. |
| §20 criterion 1 (the active manager and organization are always unambiguous) | `contradicted` | The frame makes the active manager and the organization unambiguous at all times. | The organization is: the club's name and its colour scheme are the navbar's identity band, and the header paints and titles from one read so it never settles into colour a frame after the name. The manager is never named in the chrome. | There is one manager per Save, so there is no ambiguity for a name to resolve — see *The multiplayer axis* above. The club is the career's root context and the chrome titles on it; the manager's own screen is Screen 31. |
| §10 Permissions and multiplayer behavior | `out-of-scope` | An active manager whose identity scopes what private data may be displayed, shared career data from an authoritative host or server revision, client rendering state that never grants permissions, private transient content cleared before the next manager gains control, and remote changes refreshing content without overwriting local drafts. | None of the five bullets has a referent. One manager, so nothing is scoped and nothing is handed over; no host and no server; no permission for rendering state to grant or withhold; no remote to push a change. | The multiplayer, network and multi-manager axis, removed wholesale at Group A. Disposed in full under *The multiplayer axis* above, which rests on [`CONTEXT.md`](../../../CONTEXT.md)'s **Save** entry. |
| §3 Position in the navigation model, §6 Core data model (`networkState: MultiplayerConnectionState`, `revision`, network-boundary validation), §7 Principal interactions (Back and Forward through manager-scoped routes), §8 Screen and operation states (`offline`, `permission_changed`, stale responses discarded by revision or request fingerprint), §9 Navigation behavior (manager-scoped navigation history), §11 Validation and error handling (revision validation; `permission-denied`, `offline` and `conflicted` states), §17 Persistence rules (manager-scoped drafts, authoritative career state, stale permission decisions), §18 Observability (revision conflicts), §19 Edge cases (the active manager changes, the career revision advances, the host disconnects or migrates), §20 criteria 1, 6 and 7, §21 Recommended tests (Manager switch privacy, Network disconnection) | `out-of-scope` | The active manager as a display scope, a career revision guarding against a concurrent writer, and a permission context threaded through navigation, validation, persistence and observability — plus the offline, conflicted, remote-refresh and host-migration states that go with them. | Every clause named resolves to nothing: one manager to scope to, one writer so no revision is carried or compared, one participant so no permission is checked or denied, and no network to be offline from, conflict with, or lose a host on. The sections themselves are not disposed of — the rest of §9's navigation contract, §11's validation, §17's persistence list and §19's remaining edge cases stay for this screen's audit. | All three disguises — the active manager, the career revision, the permission context — plus the network clauses they travel with. Disposed under *The multiplayer axis* above; not re-argued here. |
| §16 Security and privacy requirements (control switching, active-manager search scoping, inactive manager IDs) | `out-of-scope` | Private manager content hidden during control switching, global search results restricted by active-manager knowledge and permission, and events rejected for an inactive career or manager ID. | Control never switches, search has one knower whose permission set is total, and the one manager ID is always the active one. §16's trusted-layer route and command validation and its keep-secrets-out-of-history bullet are ordinary local hardening and stay for this screen's audit. | All three disguises — the active manager, the career revision, the permission context — plus the network clauses they travel with. Disposed under *The multiplayer axis* above; not re-argued here. |
| §22 Condensed LLM implementation brief, §23 Next planned item | `out-of-scope` | A restatement of the whole file and a pointer to the next file the import author intended to write. | Not audited. Auditing the brief would double-count every section it summarizes. | Non-normative import scaffolding — the [Group A ruling](../group_a_application_shell_and_game_lifecycle_remaining/RECONCILIATION.md), applied unchanged. |

## Screen 23: Continue and Advance Time

Status: **Reviewed** (continue-and-advance-time ticket 01, 2026-09-06).

The screen the import describes is a dialog for configuring an advance. This game's counterpart is a
single control in the career chrome and the one command behind it: the player presses Continue and the
Calendar jumps to the next scheduled event. That difference disposes of most of the file, and the rows
below say which decision each disposal rests on.

The design this screen is built to is not the import. It is
[Continue as the global career loop](../../../.agents/notes/implemented/feature/2026-08-29-continue-as-global-career-loop.md)
and [The human Fixture's pre-match boundary](../../../.agents/notes/implemented/architecture/2026-08-29-human-fixture-pre-match-boundary.md),
which are more specific than the import on every point where they overlap it.

The blanket multiplayer sweep found almost nothing left to do here: this screen's own audit had already
disposed of §9, §10, §11, §16, §17 and §19 on the rows below. Only §3 was outstanding, and it is added
rather than duplicated.

| Sections | Kind | What the spec asks | Disposition | Anchor |
|---|---|---|---|---|
| §2 Primary user goals, §7 Principal interactions, §10 Permissions and multiplayer behavior, §19 Edge cases, §20 criterion 5, §21 Recommended tests | `out-of-scope` | Coordinated advancement across participants, mark-ready and withdraw-readiness, host policies, host disconnect and migration, and tests for each. | None of it exists. | The multiplayer, network, and host axis is removed wholesale from this project: there is exactly one human manager per Save. |
| §9 Navigation behavior, §10 Permissions and multiplayer behavior, §17 Persistence rules | `out-of-scope` | An active manager whose identity scopes which private data may be displayed, manager-scoped navigation history and drafts, and transient private content cleared before another manager gains control. | There is one manager, so nothing is scoped to them and nothing is cleared between them. | Same axis. `CONTEXT.md`'s **Save** entry fixes one human manager per Save. |
| §6 Core data model, §8 Screen and operation states, §11 Validation and error handling, §15 Performance requirements, §16 Security and privacy requirements | `out-of-scope` | A career revision carried on the request, stale responses discarded by revision or request fingerprint, and advancement only from an authoritative canonical revision. | The advance is one local command against one SQLite file. No revision is carried, and there is no second writer for one to guard against. | The revision model belongs to the client-server architecture ruled out with the multiplayer axis. Single-writer local persistence is the premise of the Decider model — see [Domain-bounded Deciders](../../../.agents/notes/implemented/architecture/2026-08-27-domain-bounded-deciders-and-chunked-resimulation.md). |
| §4 Conceptual layout (`Stop at`), §2 Primary user goals, §7 Principal interactions, §20 criterion 3, §21 Recommended tests | `contradicted` | A stop-policy selector and advancement to a user-chosen future date, validated as in the future. | Neither exists. One press advances to the next scheduled event and stops there; the player chooses when to press, never where to stop. | [Calendar](../../../CONTEXT.md) — the career advances only by jumping to the next scheduled event, never by a day-by-day clock, so there is no set of dates to choose between. A date-bearing view of the schedule belongs to [Screen 28](28_calendar_and_schedule.md). |
| §4 Conceptual layout (`Next mandatory event`) | `deferred` | The control names the next mandatory event and the date it falls on, beside the current date. | The chrome carries the current date — `Season 3 · 17 Oct 2026` — but nothing names what is coming. Naming the next event means knowing which Fixture the human club plays next, which no query answers today. | [The human Fixture's pre-match boundary](../../../.agents/notes/implemented/architecture/2026-08-29-human-fixture-pre-match-boundary.md) is what gives the chrome a pending Fixture to name. |
| §2 Primary user goals, §7 Principal interactions, §8 Screen and operation states, §20 criterion 4 | `contradicted` | Cancel long processing at a safe boundary, with `queued`, `processing`, and `cancellation_requested` states and progress events. | An advance is one command that reaches the next boundary and commits, or does not. There is no queue, no progress stream, and nothing to cancel into. | [Durable at commit](../../../.agents/notes/proposed/architecture/2026-08-30-durable-at-commit-persistence.md) — every Command that succeeds has already been written, so a half-advanced Calendar is not a state the domain has. |
| §4 Conceptual layout, §7 Principal interactions | `contradicted` | A dedicated Advance Time screen holding the control, the stop policy, and the blocking list. | There is no such screen. Continue is a control in the chrome, present on every career route, and what it has to say is said beside it. | [Continue as the global career loop](../../../.agents/notes/implemented/feature/2026-08-29-continue-as-global-career-loop.md) — Continue belongs to the career, not to a screen, and a screen the player must navigate to in order to advance time is the failure that note removed. |
| §18 Observability | `out-of-scope` | Operation duration, result category, and diagnostic codes recorded as telemetry. | Nothing leaves the device. Local structured logging is unaffected and stays in scope. | The app has no backend to receive telemetry, no consent surface, and no privacy policy to link. Same axis as Group A. |
| §15 Performance requirements (virtualization, debounced search, stable row identity, scroll position) | `out-of-scope` | Virtualized long lists, debounced filters, preserved scroll position. | The control is one button and a short list of outstanding items. There is no data-heavy list on this surface to virtualize. | Template scaffolding: §15's list requirements are repeated verbatim across the group's screens and have no referent on a control that renders at most a handful of rows. |
| §13 Localization requirements, §12 Accessibility (RTL, 200 percent scaling) | `deferred` | Localized labels, dates, durations and plural forms; right-to-left layout; text scaling to 200 percent. | No i18n layer exists; every string is a hard-coded English literal, and the window is fixed-size. | `unscheduled` — the same row Group A Screen 1 carries. Localization has an obvious referent and is not an auditor's to rule out. |
| §14 Responsive behavior | `deferred` | Wide, standard, narrow and ultrawide layouts, with the primary action preserved under high text scaling. | The chrome lays out for one window size. Nothing degrades, because nothing can yet be resized. | Group A [Screen 17 Display and Sound Options](../group_a_application_shell_and_game_lifecycle_remaining/17_display_and_sound_options.md) owns resizing. |
| §11 Validation and error handling (offline, conflicted) | `out-of-scope` | Distinguish offline and conflicted states from empty, unavailable, and failed. | Neither exists to distinguish. The renderer's only remote is the main process in the same application. | Same axis as the revision model: there is no network to be offline from and no second writer to conflict with. |
| §3 Position in the navigation model | `out-of-scope` | The screen preserves the active manager, the career revision, and the permission context across navigation. | None of the three exists to preserve. Navigation history, the fourth item in the same sentence, is real and is not disposed of here. | All three disguises — the active manager, the career revision, the permission context — plus the network clauses they travel with. Disposed under *The multiplayer axis* above; not re-argued here. |
| §22 Condensed LLM implementation brief, §23 Next planned item, Suggested Git commit | `out-of-scope` | A prose restatement of the whole file, the next screen in the import's order, and a commit message. | Not audited. Auditing the brief would double-count every section it summarizes. | Non-normative import scaffolding — an artifact of how the import was generated, not a requirement. |
| §20 criterion 1, §7 Principal interactions, §21 Recommended tests (Blocked decision) | `deferred` | Mandatory decisions cannot be skipped: the advance stops before them and offers to open them. | The advance does not stop before the human club's Fixture — it resolves that Fixture headlessly and reports a count — so the one mandatory decision in the game is skippable today. | [The human Fixture's pre-match boundary](../../../.agents/notes/implemented/architecture/2026-08-29-human-fixture-pre-match-boundary.md), ticketed as its own effort. |

## Screen 24: News Inbox

Status: **Reviewed** (group-b-reconciliation ticket 02, 2026-09-12).

The single-file implementation covers all three screens (24, 25, 26) as one list-and-detail route, a
deliberate design choice its header comment explains: one route makes opening a message without losing
inbox position free rather than a restoration problem. `Reviewed` rather than `Audited` because the
same behavioural caveat applies as Screen 22 — the accessibility, localisation, and responsiveness
sections (§12, §13, §14) assert claims that a read confirms structurally but does not test at runtime.

| Sections | Kind | What the spec asks | Disposition | Anchor |
|---|---|---|---|---|
| §10 Permissions and multiplayer behavior | `out-of-scope` | An active manager whose identity scopes what private data may be displayed, shared career data from an authoritative host or server revision, client rendering state that never grants permissions, private transient content cleared before the next manager gains control, and remote changes refreshing content without overwriting local drafts. | None of the five bullets has a referent. One manager, so nothing is scoped and nothing is handed over; no host and no server; no permission for rendering state to grant or withhold; no remote to push a change. | The multiplayer, network and multi-manager axis, removed wholesale at Group A. Disposed in full under *The multiplayer axis* above, which rests on [`CONTEXT.md`](../../../CONTEXT.md)'s **Save** entry. |
| §3 Position in the navigation model, §6 Core data model (`revision`, network-boundary validation), §8 Screen and operation states (`offline`, `permission_changed`, stale responses discarded by revision or request fingerprint), §9 Navigation behavior (manager-scoped navigation history), §11 Validation and error handling (revision validation; `permission-denied`, `offline` and `conflicted` states), §17 Persistence rules (manager-scoped drafts, authoritative career state, stale permission decisions), §18 Observability (revision conflicts), §19 Edge cases (the active manager changes, the career revision advances, the host disconnects or migrates), §20 criterion 6, §21 Recommended tests (Manager switching) | `out-of-scope` | The active manager as a display scope, a career revision guarding against a concurrent writer, and a permission context threaded through navigation, validation, persistence and observability — plus the offline, conflicted, remote-refresh and host-migration states that go with them. | Every clause named resolves to nothing: one manager to scope to, one writer so no revision is carried or compared, one participant so no permission is checked or denied, and no network to be offline from, conflict with, or lose a host on. The sections themselves are not disposed of — the rest of §9's navigation contract, §11's validation, §17's persistence list and §19's remaining edge cases stay for this screen's audit. | All three disguises — the active manager, the career revision, the permission context — plus the network clauses they travel with. Disposed under *The multiplayer axis* above; not re-argued here. |
| §16 Security and privacy requirements (inbox query scoping, hot-seat inbox leakage) | `out-of-scope` | Every inbox query scoped to the active manager, and another hot-seat manager's inbox never revealed. | There is one inbox because there is one manager, so the scoped query is the unscoped query and there is no second inbox to leak. §16's untrusted-text rendering, entity-link and action-token validation, and replay prevention are local and stay for this screen's audit. | All three disguises — the active manager, the career revision, the permission context — plus the network clauses they travel with. Disposed under *The multiplayer axis* above; not re-argued here. |
| §2 Primary user goals, §4 Conceptual layout, §6 Core data model (`InboxMessageSummary` with `senderSummary`, `entityLinks`, `deadline`, `priority`) | `contradicted` | An `InboxMessageSummary` with sender summary, entity links, deadline, and priority carried as fields; the message list rendered as a table with DATE, TYPE, SUBJECT, STATE columns. | The contract type `NewsMessageView` carries `category`, `body`, `flagged`, `date`, `seasonNumber` instead — a flatter shape with no sender, entity links, or deadline. The list renders as clickable card rows with subject, date, category, state line, and badges, not the import's four-column table. | The contract shape returned by `newsInboxAtom` (RPC). The card layout is a UI choice documented at `NewsInboxScreen.tsx:33-35`. |
| §4 Conceptual layout (selected message preview or dedicated message pane), §7 Principal interactions (Previous/Next browsing) | `contradicted` | A dedicated message pane or preview beside or below the list, with Previous/Next buttons for browsing adjacent messages. | The message pane (`MessagePane`) renders inline beside the list. Previous/Next is keyboard-only (ArrowUp/ArrowDown, clamp-at-ends) — no Previous/Next buttons in the pane header. | The list-and-detail layout at `NewsInboxScreen.tsx:361`; `stepSelection` at `inboxState.ts:42-52`. |
| §7 Principal interactions (bulk-update with transactional preview), §20 criterion 4 (bulk actions affect only eligible selected messages) | `contradicted` | Bulk-update with a transactional preview step before committing; bulk actions affect only eligible selected messages. | Bulk actions (Mark all read, Archive all) commit immediately with no preview — they affect every visible eligible message (computed by `bulkTargets`). The action is a single RPC call, not a preview-then-commit flow. | `bulkTargets` at `inboxState.ts:86-103` filters by current state so actions only touch messages they would actually change. `patch` at `NewsInboxScreen.tsx:228-234` is called directly with no preview step. |
| §6 Core data model (`senderSummary`, `entityLinks`), §20 criterion 5 (deadlines and expired states) | `deferred` | Sender summary and entity links (player, club, competition) on each message; expired deadlines clearly communicated. | No sender, entity links, or explicit deadline exist on `NewsMessageView`. Expired actions are shown with a "Lapsed" label. | `unscheduled`. The contract shape would need new fields and main-process projection logic. |
| §5 Information architecture, §15 Performance requirements (virtualized rendering, debounced search, stable row identity and scroll position) | `deferred` | Virtualized long lists, debounced search, preserved row identity and scroll position across refreshes. | The list renders whole with no virtualization. Search applies synchronously in a `useMemo`. Scroll position is not preserved across a refresh. | `unscheduled`. Code comments at `NewsInboxScreen.tsx:38-41` address this explicitly: a few hundred messages per career do not justify the machinery. |
| §12 Accessibility requirements (focus restoration after linked detail screens close), §13 Localization requirements, §14 Responsive behavior | `deferred` | Focus returned when a linked detail screen closes; localized labels, dates, numbers; responsive layouts for wide, standard, narrow, and ultrawide. | No focus restoration on return from Transfers (the pane routed there navigates away). No i18n — every string is a hard-coded English literal. No responsive layout — one window size. | `unscheduled` — same row Screen 22 §12 and §14 carry. Group A Screen 17 owns resizing. |
| §8 Screen and operation states (`bulk_editing`, `offline`, `permission_changed`) | `contradicted` | Eight operation states including `bulk_editing`, `offline`, and `permission_changed`. | No `bulk_editing` state — bulk actions commit immediately. `offline` and `permission_changed` belong to the disposed multiplayer axis. | The atom-based state model (`Initial`, `Success`, `Failure` plus `inboxResult.waiting`). Bulk actions commit at `NewsInboxScreen.tsx:233`. |
| §9 Navigation behavior (Back returns to prior safe screen and restores logical focus) | `contradicted` | Back navigates to the "prior safe screen" and restores logical focus within it. | Back is the router's `history.back()` with no filtering — "prior safe screen" is the previous screen, per Screen 22's disposal. Focus restoration is the arriving screen's wrapper `div`, not a region within it. | Screen 22 §9 — the disposal of the "prior safe screen" clause. |
| §20 criterion 7 (keyboard and assistive-technology navigation is complete) | `deferred` | Full keyboard and screen-reader support for the message list and filters. | ArrowUp/Down, Home/End, Enter/Space are implemented. `aria-selected`, `aria-activedescendant`, `role="listbox"`, `role="option"`, `aria-live="polite"`, `aria-label` are present. No visible focus restoration after navigation. | `unscheduled`. The implementation has the semantic markup but the keyboard surface is proven only in isolation — the behavioural half depends on the shell's focus contract. |
| §22 Condensed LLM implementation brief, §23 Next planned item, `Suggested Git commit` | `out-of-scope` | A restatement of the whole file, a pointer to the next file the import author intended to write, and a commit message. | Not audited. Auditing the brief would double-count every section it summarizes. | Non-normative import scaffolding — the [Group A ruling](../group_a_application_shell_and_game_lifecycle_remaining/RECONCILIATION.md), applied unchanged. |

## Screen 25: Individual News Message

Status: **Reviewed** (group-b-reconciliation ticket 02, 2026-09-12).

Audited as part of the screens-24-25-26 ticket. The import's Screen 25 describes a separate route; the
implementation treats it as an inline pane of the inbox, which is consistent with the group's
structural commitment to list-and-detail in one route and with Screen 22 §9's entity-route ruling.

| Sections | Kind | What the spec asks | Disposition | Anchor |
|---|---|---|---|---|
| §10 Permissions and multiplayer behavior | `out-of-scope` | An active manager whose identity scopes what private data may be displayed, shared career data from an authoritative host or server revision, client rendering state that never grants permissions, private transient content cleared before the next manager gains control, and remote changes refreshing content without overwriting local drafts. | None of the five bullets has a referent. One manager, so nothing is scoped and nothing is handed over; no host and no server; no permission for rendering state to grant or withhold; no remote to push a change. | The multiplayer, network and multi-manager axis, removed wholesale at Group A. Disposed in full under *The multiplayer axis* above, which rests on [`CONTEXT.md`](../../../CONTEXT.md)'s **Save** entry. |
| §1 Purpose (a manager-scoped message), §3 Position in the navigation model | `contradicted` | A separate route reachable from the inbox, showing one manager-scoped message with structured content, entity links, attachments, and actions. | No separate route. Screen 25 is `MessagePane`, an inline component that renders when a message is selected from the inbox list. It has no URL, no shell entry, and no focus identity of its own — it is anchored to the inbox screen. | The list-and-detail design choice at `NewsInboxScreen.tsx:29-35`. Screen 22 §9 disposes of entity-id routes. |
| §6 Core data model (`contentBlocks`, `entityLinks`, `attachments`, `actions`, `actionHistory`, `navigationContext`, `header`) | `contradicted` | A `NewsMessageViewModel` with structured body blocks, entity links, attachments, actions with eligibility, action history, and navigation context (previous/next within inbox). | The pane receives the same flat `NewsMessageView` the list uses: `subject`, `body` (single string), `category`, `state`, `flagged`, `actionState`, `priority`, `date`, `seasonNumber`, `messageId`. No structured blocks, entity links, attachments, actions array, action history, or navigation context are modeled. | The contract shape; the pane serves the inbox's current selection rather than its own RPC call. |
| §7 Principal interactions (open a safe entity link; execute an enabled action; flag, archive, return to inbox) | `contradicted` | Safe entity links to players/clubs/competitions; enabled actions with confirmation dialogs; Flag, Archive, and Return to Inbox as pane-level affordances. | The only action target is "Answer on Transfers" routed via `navigate({ type: "transfers" })`. No entity links. Flag, Archive, and Mark Read buttons are present. No confirmation dialog. No "Back to Inbox" button — the pane is always beside the list. | `MessagePane` at `NewsInboxScreen.tsx:122-170`. The "Answer on Transfers" comment (line 153) explains the single-target design. |
| §8 Screen and operation states (`loading`, `ready`, `action_required`, `action_in_progress`, `action_completed`, `action_expired`, `unavailable`, `permission_denied`) | `contradicted` | Eight per-message states including loading, action progress, completion, expiry, unavailability, and permission denial. | No per-message state machine. The pane renders inline from the already-loaded inbox message. `action-expired` is shown as "Lapsed". `action-required` shows the "Answer on Transfers" button. `pending` prop signals in-flight patch. `permission_denied` and `unavailable` are not modeled. | The pane has no loading/ready transition — the message is already resolved from the list selection. |
| §9 Navigation behavior (Back returns to prior safe screen; entity links resolve from stable IDs; deleted entities produce unavailable state) | `contradicted` | Back returns to the inbox; entity links resolve from stable IDs; deleted entities show an unavailable state. | The pane is not a route, so it has no navigation behavior of its own. Entity links not implemented. The deleted-entity case has no referent. | Screen 22 §9 disposes of entity-id routes and the "prior safe screen" clause. Entity links are `deferred`. |
| §20 criterion 5 (Previous and Next respect the active filter result set) | `renamed` | Previous and Next buttons navigate within the current filter's result set. | Previous/Next is keyboard ArrowUp/ArrowDown, which respects the visible (filtered) list via `stepSelection(visible, ...)`. | `inboxState.ts:42-52`. |
| §22 Condensed LLM implementation brief, §23 Next planned item, `Suggested Git commit` | `out-of-scope` | A restatement of the whole file, a pointer to the next file the import author intended to write, and a commit message. | Not audited. Auditing the brief would double-count every section it summarizes. | Non-normative import scaffolding — the [Group A ruling](../group_a_application_shell_and_game_lifecycle_remaining/RECONCILIATION.md), applied unchanged. |

## Screen 26: News Filters

Status: **Reviewed** (group-b-reconciliation ticket 02, 2026-09-12).

Audited as part of the screens-24-25-26 ticket. The imports filter screen is realised as an inline
filter bar within the inbox. The main gaps (saved presets, date range, sender-type and priority
criteria, count preview) are `deferred` rather than `contradicted` because `filterNews` is already
a pure function that accepts a `NewsFilter` — widening its criteria set and adding persistence are
additive, not architectural changes.

| Sections | Kind | What the spec asks | Disposition | Anchor |
|---|---|---|---|---|
| §10 Permissions and multiplayer behavior | `out-of-scope` | An active manager whose identity scopes what private data may be displayed, shared career data from an authoritative host or server revision, client rendering state that never grants permissions, private transient content cleared before the next manager gains control, and remote changes refreshing content without overwriting local drafts. | None of the five bullets has a referent. One manager, so nothing is scoped and nothing is handed over; no host and no server; no permission for rendering state to grant or withhold; no remote to push a change. | The multiplayer, network and multi-manager axis, removed wholesale at Group A. Disposed in full under *The multiplayer axis* above, which rests on [`CONTEXT.md`](../../../CONTEXT.md)'s **Save** entry. |
| §1 Purpose (manager-scoped inbox queries), §2 Primary user goals (filter by state, category, sender type, club, nation, competition, date range; save named filter presets; preview result counts) | `contradicted` | A screen or drawer that builds reusable manager-scoped filter presets with state, action, category, sender type, entity, date range, and priority criteria, plus a result count preview. | The filter bar has: view tabs (All/Unread/Action/Flagged/Archived), category toggles (Board/Season/Transfers/Results/Development), and search text. No sender-type, club, nation, competition, date-range, or priority criteria. No saved presets. No count preview. | The filter bar at `NewsInboxScreen.tsx:284-327` and the `NewsFilter` type from `@cm-clone/shared`. The charting-spec decision that screens 24-26 are audited together applies: the filter bar is part of the inbox screen. |
| §4 Conceptual layout (dedicated filter panel with state checkboxes, date range dropdown, club/preset controls, matching count, Reset/Save/Apply buttons) | `contradicted` | A panel with state checkboxes, action checkboxes, category select, date range, club dropdown, priority dropdown, matching count, and Reset/Save Preset/Cancel/Apply buttons. | The filter section has: a tab bar for view states (single-select), toggle buttons for categories (multi-select), a search input, and "Clear filters". No matching count, date range, club/priority/sender-type filters, no save/cancel/apply distinction — filters apply immediately. | The filter bar's layout and `isNarrowed` at `inboxState.ts:81-82`. Immediate-apply is the React pattern: `setFilter` triggers a `useMemo` re-evaluation synchronously. |
| §6 Core data model (`NewsFilterDefinition` with `managerId`, `displayName`, `states`, `actionStates`, `categoryIds`, `senderTypeIds`, `entityCriteria`, `dateRange`, `priorityIds`, `revision`) | `contradicted` | A persisted filter definition with id, name, manager-scoped, carrying all criterion types, a date range object, and a revision. | The in-memory `NewsFilter` carries `view`, `categories`, and `search` only. No id, displayName, managerId, sender type, entity criteria, date range, priority, or revision. No persistence — filter state resets on screen remount. | The `NewsFilter` type in `@cm-clone/shared`; `useState` at the screen level — not persisted. |
| §7 Principal interactions (edit filter criteria; request debounced matching-count preview; apply without modifying messages; save, rename, duplicate, delete personal preset; reset to policy defaults; clear active filters) | `deferred` | Full filter management lifecycle: editing, count preview, apply action, preset CRUD, reset, and clear. | Edit (state changes apply immediately) and clear ("Clear filters" button) are the only implemented interactions. No count preview, apply-vs-preview distinction, preset CRUD, or reset to defaults. | `unscheduled`. The seam exists (`filterNews` is a pure function accepting `NewsFilter`) but the persistence and UI around it does not. |
| §8 Screen and operation states (`ready`, `modified`, `counting`, `invalid`, `saving_preset`, `conflicted`, `applying`) | `contradicted` | Seven filter-specific states including modified, counting (debounce), invalid criteria, saving preset, conflict, and applying. | No state machine. The filter is always immediately applied. The only distinction is whether it is narrowing (`isNarrowed`). No conflict, saving, counting, or invalid states. | Immediate-apply model makes `modified`/`applying` distinction meaningless. |
| §20 criterion 1 (filters affect visibility only), criterion 2 (hidden selected messages are not modified) | `contradicted` | Filters only hide results, never modify; hidden selections stay indeterminate. | Filters affect visibility only. Hidden selections: `resolveSelection` falls back to the first visible row when the selected message falls out of the filtered set. | `resolveSelection` at `inboxState.ts:27-35`. |
| §22 Condensed LLM implementation brief | `out-of-scope` | A restatement of the whole file. | Not audited. Auditing the brief would double-count every section it summarizes. | Non-normative import scaffolding — the [Group A ruling](../group_a_application_shell_and_game_lifecycle_remaining/RECONCILIATION.md), applied unchanged. |

## Screen 27: Background Processing and Updating Game

Status: **Audited** (group-b-reconciliation ticket 04, 2026-09-12).

The import describes a full-screen processing dialog with a progress bar, a task checklist, a
stop-at-safe-boundary control, and a state machine of eight states (*queued*, *processing*,
*stop_requested*, *committing_tick*, *stopped*, *completed*, *failed*, *recovery_required*). Almost
every section was already disposed of by existing rulings. The residual question — what the player sees
when an advance fails — is now answered: `ContinueResultBand` in the chrome reports a `failure` kind
with a message, a `Dismiss` button, and a red border, so the failure path is no longer silent. See
[continue-and-advance-time](../../../.scratch/continue-and-advance-time/map.md) and
`ContinueResult.tsx:27-29`.

| Sections | Kind | What the spec asks | Disposition | Anchor |
|---|---|---|---|---|
| §10 Permissions and multiplayer behavior | `out-of-scope` | An active manager whose identity scopes what private data may be displayed, shared career data from an authoritative host or server revision, client rendering state that never grants permissions, private transient content cleared before the next manager gains control, and remote changes refreshing content without overwriting local drafts. | None of the five bullets has a referent. One manager, so nothing is scoped and nothing is handed over; no host and no server; no permission for rendering state to grant or withhold; no remote to push a change. | The multiplayer, network and multi-manager axis, removed wholesale at Group A. Disposed in full under *The multiplayer axis* above, which rests on [`CONTEXT.md`](../../../CONTEXT.md)'s **Save** entry. |
| §1 Purpose (network state synchronises; simulation progress during advance), §2 Primary user goals (understand processing stage; monitor date advancement; review queued/completed tasks; cancel at a safe boundary; see why paused or stopped; inspect warnings), §4 Conceptual layout (progress bar, task checklist, stage list, stop-at-safe-boundary, show-details toggle), §6 Core data model (`CareerProcessingSnapshot` with `stages`, `overallProgress`, `canRequestStop`, `stopReason`, `warnings`), §7 Principal interactions (observe weighted progress; request stopping; open blocking message; return automatically on completion), §8 Screen and operation states (`queued`, `processing`, `stop_requested`, `committing_tick`, `stopped`, `completed`, `failed`, `recovery_required`), §20 criterion 1 (progress corresponds to active processing transaction), criterion 2 (career date never advances from renderer state), criterion 3 (stop requests at safe boundaries only), criterion 4 (completion creates consistent new revision), criterion 7 (progress announcements rate-limited), §21 Recommended tests (one-day processing, multiple fixtures, safe stop, failure and rollback, duplicate processing, progress event spoofing) | `contradicted` | A full-screen background processing dialog with a weighted progress bar, a task checklist with per-stage visibility, a stop-at-safe-boundary control, an expandable-details region, eight named processing states, and tests for cancellation, rollback, duplicate processing, and progress spoofing — all for the "during" phase of an advance, separate from its result. | No such screen or dialog exists. Continue is one command in the chrome; the advance is not a multi-step stream that a screen could observe mid-flight, and there is nothing to cancel into. The during-advance surface the import describes was already disposed of by Screen 23's rows: no dedicated Advance Time screen, no queue or progress stream, no cancellation states, and no stop-policy selector with its own state machine. The result *after* the advance — what the import's §2 "see why processing paused or stopped" asks for — is now covered by `ContinueResultBand`: it reports a `failure` kind carrying a message and a `Dismiss` button (`ContinueResult.tsx:27-29`, owned by continue-and-advance-time). The during-advance states and cancellability remain absent, and no ticket plans to introduce them. | Screen 23's rows on §4 (stop-policy selector — contradicted), §4/§7 (dedicated Advance Time screen — contradicted), §2/§7/§8/§20 criterion 4 (cancel states — contradicted). The [`ContinueResult.tsx`](../../../apps/desktop/src/renderer/chrome/ContinueResult.tsx) implementation covers the post-advance failure report. The during-advance processing model is out of scope by the same reasoning that keeps the Calendar from being a stream: gainsaying it was the premise of [Durable at commit](../../../.agents/notes/proposed/architecture/2026-08-30-durable-at-commit-persistence.md). |
| §1 Purpose (worker pools, memory budgets, resource-policy tuning), §3 Position in the navigation model (the worker and resource clauses), §15 Performance requirements (virtualization of worker event lists, debounced worker event filters, resources as a queryable surface) | `out-of-scope` | Worker pools, memory budgets, resource-policy tuning, and a queryable resource surface, including lists of processing events that would need virtualization. | Nothing exists. Worker pools and resource policies are imported from the Group A ruling and have no referent in this codebase. The performance clauses (§15) travel with them because the import's virtualized lists presuppose a surface (worker event lists) that does not exist. | Group A inheritance: worker pools, memory budgets, and resource-policy tuning were ruled out at charting (see the [map's Standing decisions](../../../.scratch/group-b-reconciliation/map.md)) and were never ticketed in this codebase. |
| §16 Security and privacy requirements (locks and idempotency keys) | `contradicted` | Prevent duplicate processing through locks and idempotency keys; accept progress only for the active processing ID; run canonical simulation in the trusted engine; use deterministic random streams; do not expose hidden match data before results are committed. | Duplicate processing has no referent: the Calendar advance is a single command that reaches the next boundary or does not, not a multi-step transaction that could be applied twice. The command is idempotent: an advance from state S to the next event E, replayed from S, produces the same E. Deterministic streams, trusted engine, and hidden-data protection are followed by construction — the match engine is seeded and runs in `packages/game-engine`, which the main process invokes, and the knowledge model keeps hidden attributes off wire projections. No §16 clause is violated; most are satisfied without an explicit lock or idempotency key because they presuppose a level of concurrency that does not exist. | [Durable at commit](../../../.agents/notes/proposed/architecture/2026-08-30-durable-at-commit-persistence.md) — every Command that succeeds has already been written, so a half-applied advance is not a state the domain has, and `continue-and-advance-time`'s single-flight guard (`CareerStateProvider.tsx`) prevents a second press before the first resolves. |
| §18 Observability (remaining: local structured logging) | `Contradicted` | Record operation duration, result category, and diagnostic codes; avoid recording private message content, manager names, or local paths. | Local structured logging exists and stays in scope. The import's prohibition on recording private data (message content, manager names, paths) is followed. The telemetry clause (sending data off-device) was disposed of by the blanket §18 row on Screen 23. | The blanket §18 row on Screen 23 disposes of the telemetry half. The local-logging half is followed; no row is needed to say it is followed, but the distinction is stated here so no future reader infers logging is absent. |
| §9 Navigation behavior (remaining after sweep: stable entity IDs, unavailable state for deleted entities, no unsafe transient dialogs restored after load), §11 Validation and error handling (remaining after sweep: preserve last valid view, distinguish empty/unavailable/failed states, corrective actions, no raw stack traces), §19 Edge cases (remaining after sweep: filters hide selected item, resume after sleep or network loss, same action submitted twice, late response after navigation) | `contradicted` | Stable entity-link IDs, an unavailable state for deleted entities, no restoration of unsafe transient dialogs, preserve last valid view on refresh failure, distinguish empty from unavailable from failed, offer Retry/Clear/Return/Open Recovery, handle filter-hidden selection, sleep/wake, duplicate submission, and late responses. | No §9 clause has a referent: no entity links exist on or from this surface (Screen 22 §9). The remaining §11 validation clauses (preserve last view, corrective actions) are part of the chrome's general validation gap recorded as `deferred` on Screen 22 §11 and no screen-specific row adds anything. The remaining §19 edge cases (filter-hidden, sleep, duplicate, late response) are likewise chrome-level, not screen-specific — no screen-level state distinguishes them. | Screen 22 §9 (entity ID routes disposed), Screen 22 §11 (validation gap deferred). This screen has no entity display, no filter, and no per-screen state machine to get out of step with, so each clause resolves to the absence of the thing it guards. |
| §5 Information architecture, §12 Accessibility requirements, §13 Localization requirements, §14 Responsive behavior, §15 Performance requirements (remaining after worker/resource disposal), §20 criterion 8 (no proprietary processing messages), §21 Recommended tests (remaining: reduced motion) | `deferred` | IA with persistent headings, labelled controls, predictable content region and status area; keyboard/screenreader; localization; responsive layout; virtualized lists (already disposed by worker-resource row); fallback for proprietary messages; reduced-motion tests. | None implemented. The surface the import describes does not exist, so none of these apply. The rows exist to state the deferral rather than leave the silence ambiguous. | `unscheduled` — same deferred pattern as Screen 22 §12/§13/§14/§15, applying to a surface that does not exist. |
| §22 Condensed LLM implementation brief, §23 Next planned item, `Suggested Git commit` | `out-of-scope` | A restatement of the whole file, a pointer to the next file the import author intended to write, and a commit message. | Not audited. Auditing the brief would double-count every section it summarizes. | Non-normative import scaffolding — the [Group A ruling](../group_a_application_shell_and_game_lifecycle_remaining/RECONCILIATION.md), applied unchanged. |

## Screen 28: Calendar and Schedule

Status: **Reviewed** (group-b-reconciliation ticket 05, 2026-09-12).

**Fixtures is the Calendar screen under another name.** The import describes a rich calendar surface
with month/week/agenda views, reminders, event-type filters, and a full event taxonomy. The existing
`FixturesScreen.tsx` groups match fixtures by date under date headers, which is the calendar-in-miniature
the game has. It serves the import's core goal ("review upcoming fixtures and deadlines, navigate by
date") under the "fixtures" vocabulary rather than "calendar". The import's richer features — view
switching, reminders, non-fixture events — would enhance Fixtures rather than create a new screen.

| Sections | Kind | What the spec asks | Disposition | Anchor |
|---|---|---|---|---|
| §1 Purpose (fixtures, deadlines, meetings, transfer windows, registration dates, draws, reminders, manager-created notes — calendar events beyond match fixtures), §4 Conceptual layout (month grid, day-detail panel, filter and reminder controls), §6 Core data model (`CalendarEventSummary` with `eventTypeId`, `allDay`, `mandatory`, `linkedEntities`, `managerVisibility`, `status`), §7 Principal interactions (change month/week/day/agenda view; jump to date; filter event categories; open linked entity; create/edit/remove reminder; advance time to selected date), §8 Screen and operation states (`loading`, `ready`, `empty_day`, `filtering`, `editing_reminder`, `refreshing`, `conflicted`), §20 criterion 2 (views represent same event set consistently), criterion 3 (manager reminders remain private), criterion 4 (filters do not delete hidden events), criterion 5 (postponed/cancelled events explicit), criterion 7 (keyboard calendar navigation complete) | `contradicted` | A full calendar surface with month-grid view, day-detail event list, per-event type classification (fixture, deadline, meeting, reminder, draw), filterable by event category, with reminder CRUD and linked entity navigation — plus a distinct state machine for calendar-specific states (empty_day, filtering, editing_reminder). | FixturesScreen shows match events grouped by date with date-header cards in a simple list — no month grid, no day-detail panel, no event-type taxonomy (every event is a fixture), no reminder capability, no view switching, no per-event status beyond played/unplayed, no date-jump control. Continue advances time to the next event, not to a user-chosen date. Reminders do not exist anywhere in the domain. FixturesScreen's state model is the atom lifecycle (Initial/Success/Failure + waiting) with no calendar-specific states. | `FixturesScreen.tsx:75-100` — the date-grouped card layout and the comment at line 51-52 that names Fixtures as the calendar surface. The atom-based state model at lines 9-47. |
| §2 Primary user goals (review upcoming fixtures and deadlines; navigate to a specific date) | `renamed` | Review upcoming fixtures and deadlines by date; navigate to a specific date via calendar controls. | FixturesScreen shows all upcoming fixtures grouped by date, rendering `formatCalendarDate` as headers — the "review by date" goal is served. A specific-date jump (without scrolling) is absent but is a UX enhancement within the existing Fixtures surface, not a new screen. | `FixturesScreen.tsx:75-80` — date-grouped rendering with `formatCalendarDate`. The Continue control advances to the next event but does not select a date; a date-picker addition would extend Fixtures, not create a Calendar screen. |
| §2 Primary user goals (filter event categories; create manager-private reminders; identify conflicts and mandatory deadlines); §6 Core data model (reminder scoping, calendar event status model); §7 Principal interactions (create, edit, remove private reminder); §16 Security and privacy (date/entity-ID validation, markup injection, mandatory-event precedence) | `contradicted` | Event category filters beyond fixture type, manager-private reminders across the event set, conflict and deadline identification, and validation of date boundaries and entity links for reminder and event data. | No event categories exist beyond "fixture" — the fixture list is undifferentiated. Reminders do not exist in the domain at all. Conflicts and mandatory deadlines have no referent: Continue resolves every event it encounters, so there is no overlapping-event concept to detect. The §16 residue (date validation, markup protection, mandatory-event guard) protects a calendar surface that does not exist and would apply when Fixtures gains non-fixture data. | The fixture-only event model (`@cm-clone/shared` fixture types). `CONTEXT.md`'s **Calendar** defines it as the advance mechanism, not an event catalogue. The §16 residue (date/entity-ID validation, markup, mandatory precedence) stays as a deferred design obligation for any future surface work. |
| §10 Permissions and multiplayer behavior | `out-of-scope` | An active manager whose identity scopes what private data may be displayed, shared career data from an authoritative host or server revision, client rendering state that never grants permissions, private transient content cleared before the next manager gains control, and remote changes refreshing content without overwriting local drafts. | None of the five bullets has a referent. One manager, so nothing is scoped and nothing is handed over; no host and no server; no permission for rendering state to grant or withhold; no remote to push a change. | The multiplayer, network and multi-manager axis, removed wholesale at Group A. Disposed in full under *The multiplayer axis* above, which rests on [`CONTEXT.md`](../../../CONTEXT.md)'s **Save** entry. |
| §3 Position in the navigation model (multiplier clauses), §6 Core data model (`revision`, network-boundary validation), §8 Screen and operation states (`conflicted`, stale responses), §9 Navigation behavior (manager-scoped history), §11 Validation and error handling (revision/offline/conflicted), §17 Persistence rules (manager-scoped drafts, stale permission decisions), §18 Observability (revision conflicts), §19 Edge cases (active manager changes, revision advances, host disconnect/migrate), §21 Recommended tests (Manager switch privacy) | `out-of-scope` | The active manager as a display scope, a career revision guarding against a concurrent writer, and a permission context threaded through navigation, validation, persistence and observability — plus the offline, conflicted, remote-refresh and host-migration states that go with them. | Every clause named resolves to nothing: one manager to scope to, one writer so no revision is carried or compared, one participant so no permission is checked or denied, and no network to be offline from, conflict with, or lose a host on. The sections themselves are not disposed of — the rest of each stays for this screen's audit. | All three disguises — the active manager, the career revision, the permission context — plus the network clauses they travel with. Disposed under *The multiplayer axis* above; not re-argued here. |
| §16 Security and privacy (reminder scoping, revision checks) | `out-of-scope` | Private reminders scoped to the active manager, and revision checks on rescheduling and multiplayer updates. | One manager, so a private reminder is private to nobody in particular; no revision and no multiplayer update to check against. The §16 bullets that survive — date/entity-ID validation, markup injection, mandatory-event precedence — are addressed in the contradicted row above. | The multiplayer axis, which removes the manager scope that reminder privacy presupposes. The surviving §16 bullets are listed as contradicted because they protect a surface that does not exist. |
| §9 Navigation behavior (remaining: stable entity IDs, unavailable state for deleted entities, no unsafe transient dialogs restored after load), §11 Validation and error handling (remaining: preserve last valid view, distinguish empty/unavailable/failed, corrective actions, no stack traces), §19 Edge cases (remaining: filters hide selected item, resume after sleep, same action submitted twice, late response) | `contradicted` | Entity-link IDs resolved from stable ids; unavailable state for deleted entities; no restoration of unsafe transient dialogs; preserve last valid view; distinguish empty/unavailable/failed; corrective actions; handle sleep/wake, duplicate, late response. | No §9 clause has a direct referent: FixturesScreen has no entity links (Screen 22 §9). The §11/§19 remaining clauses are chrome-level patterns with no screen-specific implementation. | Screen 22 §9 (entity-id routes disposed). Screen 22 §11 (validation gap deferred). This surface has no entity display, filter, or per-screen state machine, so each clause resolves to the absence of the thing it would guard. |
| §5 Information architecture, §12 Accessibility requirements, §13 Localization requirements, §14 Responsive behavior, §15 Performance requirements, §20 criterion 8 (no proprietary calendar styling), §21 Recommended tests (remaining: keyboard navigation, date boundary, deleted linked entity) | `deferred` | IA with persistent headings, labelled controls, status area; keyboard/screenreader; localization; responsive layout; virtualized lists; fallback for proprietary styling; keyboard and date-boundary and deleted-entity tests. | None implemented. FixturesScreen uses one fixed layout with no i18n, no responsive breakpoints, no virtualization (it renders at most a few dozen cards), and no keyboard navigation beyond Tab through the list. | `unscheduled` — same deferred pattern as Screen 22 §12/§13/§14/§15. The Fixtures screen would inherit any chrome-level i18n/responsive work when it lands. |
| §22 Condensed LLM implementation brief, §23 Next planned item, `Suggested Git commit` | `out-of-scope` | A restatement of the whole file, a pointer to the next file the import author intended to write, and a commit message. | Not audited. Auditing the brief would double-count every section it summarizes. | Non-normative import scaffolding — the [Group A ruling](../group_a_application_shell_and_game_lifecycle_remaining/RECONCILIATION.md), applied unchanged. |

## Screen 29: Manager Notebook

Status: **Disposed in full** (ruled at charting, group-b-blanket-disposals ticket 01, 2026-09-07).

The screen was ruled out of scope as a whole file before any Group B ticket opened, on two grounds that
hold independently: the notebook itself has no referent in this game, and the privacy model it is built
on is the multi-manager one Group A removed. Either alone disposes of the file. The single row below
covers every section, so nothing here is silent and no audit of this screen is owed.

| Sections | Kind | What the spec asks | Disposition | Anchor |
|---|---|---|---|---|
| §1–§23 and Suggested Git commit — the whole file | `out-of-scope` | A manager-private notebook: free-prose notes with tags and pinning, annotations linked to players, staff, clubs, competitions, fixtures and dates, search and filter over them, note-to-reminder conversion, and export or deletion under a privacy policy. | Nothing exists and nothing is planned. There is no note, tag, pin, annotation, or reminder anywhere in the domain. | Two independent grounds. **First**, the note concept is an import invention: it appears in no code, in no `CONTEXT.md` term, and in no recorded decision, and nothing in the game asks the player to keep private prose — the player's record of a career is the career state itself. **Second**, the screen takes manager-private data as its premise, and privacy between managers presupposes more than one; [`CONTEXT.md`](../../../CONTEXT.md)'s **Save** entry fixes exactly one human manager per Save, and the multiplayer, network and multi-manager axis was removed wholesale from this project at Group A. Returns only if this game acquires a reason for the player to write prose the game itself does not model — a new effort against a redrawn scope, not a resumption of this one. Overturning the multiplayer ruling alone would not bring it back, because the first ground would still stand. |

## Screen 30: Manager History

Status: **Reviewed** (group-b-reconciliation ticket 06, 2026-09-12).

The import describes a multi-club career timeline (appointments, resignations, dismissals, honours,
awards, milestones) that does not apply to this game's single-club-per-Save model. But the question
underneath — does a Save accumulate a season-by-season record — is real and has a positive answer.

**The career record exists, partially, and Season Summary is its surface.** Per-season league positions
and Verdicts persist in `board_objective` (one row per season). The lifetime `manager_status` row
carries `consecutive_misses`, `last_outcome`, and `archived_cause`. Season Summary already shows the
most recent season; an "all seasons" view would extend it rather than create a new screen. Manager
Profile is not the home: the Group A Agent Note reserves it for creation-time identity only. Honours
and cross-season match-count aggregates are not computed and are deferred.

| Sections | Kind | What the spec asks | Disposition | Anchor |
|---|---|---|---|---|
| §1 Purpose (chronological record of appointments, resignations, dismissals, retirements, honours, awards, milestones), §2 Primary user goals (review career timeline, inspect completed seasons, view honours and history), §4 Conceptual layout (career timeline, season-by-season position, honour list), §6 Core data model (`ManagerHistoryViewModel` with appointment records, honours, awards, career milestones, exportable summary), §7 Principal interactions (scroll timeline, open past season, open honour details, export record), §20 criterion 1 (chronological history), criterion 2 (history matches canonical career state), criterion 3 (honours derived from results) | `contradicted` | A multi-appointment career timeline across clubs, with per-season league positions, cumulative match record (wins, draws, losses), honours and awards listed chronologically, exportable career summary, and each honour linked to its source result. | None of the import's surface exists as described. Season Summary shows the most recent season's position and verdict only — there is no multi-season view, no cumulative match record, no honour list, no export, and no career timeline beyond the current-season single entry. The data to build a per-season position view exists (past seasons' `board_objective` rows persist), but no query or surface reads it. Honours, match aggregates, and export operate on data that is not computed. The multi-appointment clause is additionally out of scope because there is one club per Save. | Season Summary's single-season query at `season/queries.ts:100-144` (reads `board_objective` with `ORDER BY verdict IS NULL ASC, season_number DESC LIMIT 1`). The data for past seasons is in the same table but the query does not reach it. The career-record decision at ticket 06 states which data exists and where its surface belongs. |
| §2 Primary user goals (review history and retirement details), §20 criterion 4 (private history separate from public) | `out-of-scope` | Private inbox and disciplinary notes kept out of the public history view; retirement visible without private data. | There is one reader and everything the game holds is shown to them. The private-versus-public split presupposes a second reader, so there is no public view to redact into. | All three disguises — the active manager, the career revision, the permission context. Disposed under *The multiplayer axis* above; not re-argued here. |
| §10 Permissions and multiplayer behavior | `out-of-scope` | An active manager whose identity scopes what private data may be displayed, shared career data from an authoritative host or server revision, client rendering state that never grants permissions, private transient content cleared before the next manager gains control, and remote changes refreshing content without overwriting local drafts. | None of the five bullets has a referent. One manager, so nothing is scoped and nothing is handed over; no host and no server; no permission for rendering state to grant or withhold; no remote to push a change. | The multiplayer, network and multi-manager axis, removed wholesale at Group A. Disposed in full under *The multiplayer axis* above, which rests on [`CONTEXT.md`](../../../CONTEXT.md)'s **Save** entry. |
| §3 Position in the navigation model (multiplier clauses), §6 Core data model (revision-immutable models, network-boundary validation), §8 Screen and operation states (stale responses discarded by revision or fingerprint), §9 Navigation behavior (manager-scoped navigation history), §11 Validation and error handling (revision validation; permission-denied, offline, conflicted), §17 Persistence rules (manager-scoped drafts, stale permission decisions), §18 Observability (revision conflicts), §19 Edge cases (active manager changes, revision advances, host disconnect/migrate), §21 Recommended tests (Export privacy) | `out-of-scope` | The active manager as a display scope, a career revision guarding against a concurrent writer, and a permission context threaded through navigation, validation, persistence and observability — plus the offline, conflicted, remote-refresh and host-migration states that go with them. | Every clause named resolves to nothing: one manager to scope to, one writer so no revision is carried or compared, one participant so no permission is checked or denied, and no network to be offline from. The sections themselves are not disposed of — the rest of each (entity links, validation, remaining edge cases) stays for classification in this row. | All three disguises — the active manager, the career revision, the permission context — plus the network clauses they travel with. Disposed under *The multiplayer axis* above. |
| §16 Security and privacy (append-only history, entity-visibility, safe text, no client-editing) | `contradicted` | Career history is append-only; entity visibility respects the knowledge model; safe text rendering; no client-side editing of the record. | Append-only and no client-editing are followed by construction — the career record is event-sourced and the renderer has no write capability for it. Entity-visibility (the knowledge model) and safe text (markup protection) would apply if a surface existed; they are not violated and no surface exists to violate them. | The event-sourced decider model ([Domain-bounded Deciders](../../../.agents/notes/implemented/architecture/2026-08-27-domain-bounded-deciders-and-chunked-resimulation.md)) and the renderer's RPC-only write path. |
| §9 Navigation behavior (remaining: stable entity IDs, unavailable state for deleted entities, no unsafe transient dialogs restored after load), §11 Validation and error handling (remaining: preserve last valid view, distinguish empty/unavailable/failed, corrective actions), §19 Edge cases (remaining: filters hide selected, sleep/wake, duplicate submission, late response) | `contradicted` | Entity-link IDs from stable ids; unavailable state for deleted entities; preserve last view; distinguish empty/unavailable/failed; corrective actions; sleep/wake handling. | No §9 clause has a direct referent — there are no entity links on this surface. The remaining §11/§19 clauses are chrome-level patterns with no screen-specific implementation. | Screen 22 §9 (entity-id routes disposed). Screen 22 §11 (validation gap deferred). |
| §5 Information architecture, §12 Accessibility requirements, §13 Localization requirements, §14 Responsive behavior, §15 Performance requirements, §20 criterion 5 (export is deterministic), criterion 6 (proprietary data not used), §21 Recommended tests (remaining: keyboard navigation, date boundary) | `deferred` | IA with headings, labelled controls, status area; keyboard/screenreader; localization; responsive layout; virtualized lists for honours list; export determinism; no proprietary artwork; keyboard and date tests. | None implemented. The surface the import describes does not exist. Multi-season position history is data that exists but has no query or surface; the deferred row names the missing surface and owns its implementation via Season Summary extension. | `unscheduled` — matches Screen 22's deferred pattern for §12/§13/§14/§15. The cross-season position view is the one piece with a data referent; the rest presupposes an honour/match-count model that does not exist. |
| §22 Condensed LLM implementation brief, §23 Next planned item, `Suggested Git commit` | `out-of-scope` | A restatement of the whole file, a pointer to the next file the import author intended to write, and a commit message. | Not audited. Auditing the brief would double-count every section it summarizes. | Non-normative import scaffolding — the [Group A ruling](../group_a_application_shell_and_game_lifecycle_remaining/RECONCILIATION.md), applied unchanged. |

## Screen 31: Manager Profile

Status: **Reviewed** (group-b-reconciliation ticket 03, 2026-09-12 — complement to Group A's ticket 06).

Read as a complement to Group A's Manager Profile screen (ticket 06). The core design — identity (name,
Archetype, Pillars), club context (name, Season, tenure), active/archived badge, career-screen
placement — is settled and not re-litigated here. What follows are rows for every addition the import
makes beyond that decision.

The profile currently holds: manager name, Archetype origin (or Custom Manager), four Manager Pillar
values, club name, Season number, tenure length, and an active/archived badge. That is the full extent
of `ManagerProfileScreenView`. No languages, qualifications, background, reputation, familiarity,
relationships, career record, honours, contract details, or tab navigation exist on this screen.

The career-record question (§2 "Review career record, honours, and history", §4 career-record stat row,
§6 `careerRecord`/`honours`) is the one genuinely open piece and is handed to ticket 06 unresolved.

| Sections | Kind | What the spec asks | Disposition | Anchor |
|---|---|---|---|---|
| §1 Purpose (languages, relationships, reputation, background, public history), §4 Conceptual layout (Nationality, Languages, Qualification, Relationship tabs), §6 Core data model (background, communication, relationships, reputation) | `contradicted` | Languages, qualifications, a background narrative, relationships to other actors, a reputation metric, and familiarity with clubs/leagues as manager-level attributes on the profile. | None of these is modelled. The identity model is Archetype and Pillars — a one-time creation choice with no languages, qualifications, background narrative, relationships, reputation, or familiarity beyond Position Familiarity (which is player-facing, not manager-facing). Nationality exists in [`CONTEXT.md`](../../../CONTEXT.md) as a player generation input only; no manager nationality is stored or displayed. | [`CONTEXT.md`](../../../CONTEXT.md) — **Manager Archetype**, **Manager Pillar**, **Manager Profile**. The identity model was settled at Group A's ticket 06 (the [`Manager Profile` Agent Note](../../../.agents/notes/implemented/feature/2026-08-30-manager-profile-screen.md)) and is not re-opened. |
| §2 Primary user goals (open current club, contract, and related entities), §4 Conceptual layout ([Open Club] button, [Contract] tab), §7 Principal interactions (open club, competition, contract; Back and Forward through profile tabs) | `contradicted` | Club entity link, contract detail screen, and sub-navigation through profile tabs — each reachable from this screen. | The club context Card (`ManagerProfileScreen.tsx:211-218`) displays the club name, Season number and tenure as static text. There is no entity link to a club page, no contract detail screen, and no tab navigation — the route model has no entity-id routes (Screen 22 §9). `navigateBack` is the router's unfiltered `history.back()` with no sub-views to navigate between. | Screen 22 §9 — no career destination in `apps/desktop/src/renderer/navigation/destinations.ts` carries an entity id. [`CONTEXT.md`](../../../CONTEXT.md)'s **Contract** has no screen to open. |
| §2 Primary user goals (access authorized actions: notebook, ownership, resignation, retirement), §4 Conceptual layout ([Notebook] [Manager Status] [Actions] buttons), §7 Principal interactions (open manager notebook; open permitted lifecycle actions) | `contradicted` | Notebook, ownership actions, resignation, retirement, and a generic "Manager Status" entry as actionable items from the profile. | Notebook is Screen 29, disposed in full. Ownership presupposes a second manager to hold something distinct from: one manager owns everything. Resignation and the job market are Group A rulings deferred to Group N. "Manager Status" is a retired term (the screen is Manager Profile, not Manager Status — see Agent Note). The only lifecycle action that exists is Retire Manager, implemented as a destructive button with an Irreversibility Disclosure dialog at `ManagerProfileScreen.tsx:234-248`. | Screen 29's full-file disposal; Group A's inheritance of resignation (Group N); the [`Manager Profile` Agent Note](../../../.agents/notes/implemented/feature/2026-08-30-manager-profile-screen.md) retiring "Manager Status". The retire action's implementation is at `ManagerProfileScreen.tsx:45-114`. |
| §4 Conceptual layout (tab bar: Overview, Career Record, History, Relationships, Contract) | `contradicted` | A multi-tab profile with Overview, Career Record, History, Relationships and Contract as named tabs, each holding a distinct content region. | The implementation is a single-view scrolling layout with no tabs: a heading section (name + badge + archetype), a Club card, a Management Philosophy card, and the retire action. Tabs have no counterpart — the data partitions the import would separate do not exist as distinct queryable surfaces. | `ManagerProfileScreen.tsx:191-258` — the screen's render structure. The `ManagerProfileScreenView` contract carries no tab state, tab selection, or tab identifiers. |
| §4 Conceptual layout (career record row: matches, wins, draws, losses), §6 Core data model (`careerRecord`, `honours`) | — | A career record aggregate with match counts (wins, draws, losses) and a list of honours. | **The one genuinely open question.** Whether a Save accumulates a season-by-season career record, and whether that record belongs on Manager Profile, on a dedicated screen, or on Season Summary, is the subject of ticket 06. Not settled here. | Handed to [`06-screen-30-does-a-save-accumulate-a-career-record.md`](../../../.scratch/group-b-reconciliation/issues/06-screen-30-does-a-save-accumulate-a-career-record.md) unresolved. |
| §4 Conceptual layout (Contract details: "Contract to June 2007", "Board confidence: Stable") | `contradicted` | Contract length, expiry date, and board confidence as visible profile data. | No contract data is shown. Contract length is modelled in [`CONTEXT.md`](../../../CONTEXT.md) as a {wage, length} pair but has no screen surface; the concept of a visible expiry date or renewal status does not exist. Board confidence is a Season-boundary judgment (Board Objective → Verdict → Consecutive-Miss Counter) owned by Season Summary, not profile identity. | [`CONTEXT.md`](../../../CONTEXT.md) — **Contract**, **Board Objective**, **Verdict**, **Consecutive-Miss Counter**. |
| §6 Core data model (`ManagerProfileViewModel` with `managerId`, `revision`, `identity`, `employment`, `background`, `communication`, `reputation`, `careerRecord`, `honours`, `relationships`, `permittedActions`) | `contradicted` | A rich profile view model with identity, employment, background, communication, reputation, career record, honours, relationships, and a permitted-actions list. | The contract type `ManagerProfileScreenView` carries `profile` (name, archetypeOrigin, pillars), `clubName`, `seasonNumber`, `tenureSeasons`, and `archived`. None of the import's aggregate fields exist — the contract is deliberately narrow per the Group A decision: profile identity and club context only. | The `ManagerProfileScreenView` contract at `packages/contracts` — settled at Group A's ticket 06 and documented in the [`Manager Profile` Agent Note](../../../.agents/notes/implemented/feature/2026-08-30-manager-profile-screen.md) (see "Screen content" and "What does NOT belong here"). |
| §8 Screen and operation states (`loading`, `ready`, `retired`, `unemployed`, `permission_limited`, `refreshing`, `unavailable`) | `contradicted` | Seven named screen states including retired, unemployed, and permission_limited as distinct operational states. | The screen has four states from its atom-based model: `Initial` (loading), `Success` with an `active` or `archived` badge, `Failure`, and `waiting` (refreshing). A retired/archived save renders the same profile with an `[Archived]` banner and hides the Retire button; it is a display treatment, not a distinct state machine. There is no unemployed state — the manager is always employed by the one club per Save, and there is no job market. `permission_limited` belongs to the disposed axis. | `ManagerProfileScreen.tsx:149-170` (state rendering) and the `retireManagerMutation` (archival path). The atom model's three-state lifecycle (`Initial`/`Success`/`Failure`), plus `waiting`. |
| §16 Security and privacy requirements (hidden attributes: "Do not expose hidden low-level attributes beyond policy") | `contradicted` | Hidden attributes (Potential Ability, Injury Proneness, etc.) are never exposed on any profile surface. | The manager has no hidden attributes — the `manager_profile` table stores name, Archetype origin, and Pillar values, none of which is hidden. The screen reads no player Attributes, Potential Ability, or Injury Proneness. The rule is silently satisfied: there is nothing hidden to expose, and the contract shape physically prevents reaching one. | Resolves the §16 residue the blanket row reserved for this ticket. The rule is followed by construction — the data `ManagerProfileScreenView` carries has no hidden dimension. The `ManagerProfileScreenView` contract at `packages/contracts`. |
| §20 criterion 8 (no proprietary profile artwork required), §21 Recommended tests (Portrait fallback) | `contradicted` | Portrait fallback; the profile renders safely without a portrait asset. | The screen has no portrait at all. No portrait slot, no image element, no fallback — and therefore no proprietary-artwork risk. | `ManagerProfileScreen.tsx:202-209` — the heading section renders name and badge only, with no `img` element. |
| §5 Information architecture, §12 Accessibility requirements, §13 Localization requirements, §14 Responsive behavior, §15 Performance requirements (remaining clauses), §19 Edge cases (remaining), §21 Recommended tests (remaining) | `deferred` | Information hierarchy and behaviour, keyboard/screenreader access, localization, responsive layout, high-contrast and text scaling, virtualized rendering, stable row identity, scroll preservation, and edge cases for portrait, contract, employment, and the Archived state. | None of it is implemented. The screen uses one fixed layout, English literals, no i18n, no responsive breakpoints, no virtualization (it is too short to need it), and no focus restoration beyond the arriving screen's wrapper div. | `unscheduled` — same row Screen 22 §12, §13, §14 and §15 carry. Group A Screen 17 owns resizing. |
| §22 Condensed LLM implementation brief, §23 Next planned item, `Suggested Git commit` | `out-of-scope` | A restatement of the whole file, a pointer to the next file the import author intended to write, and a commit message. | Not audited. Auditing the brief would double-count every section it summarizes. | Non-normative import scaffolding — the [Group A ruling](../group_a_application_shell_and_game_lifecycle_remaining/RECONCILIATION.md), applied unchanged. |

## Screen 32: Manager Chat and Multiplayer Communication

Status: **Disposed in full** (ruled at charting, group-b-blanket-disposals ticket 01, 2026-09-07).

The screen was ruled out of scope as a whole file before any Group B ticket opened. It is the
multiplayer, network and multi-manager axis end to end, with no residue on any other axis, so a
section-by-section pass would restate the same disposal twenty-three times. The single row below
covers every section, so nothing here is silent and no audit of this screen is owed.

| Sections | Kind | What the spec asks | Disposition | Anchor |
|---|---|---|---|---|
| §1–§23 and Suggested Git commit — the whole file | `out-of-scope` | Career-scoped communication between participants: a lobby with manager presence, public, team, direct and system channels, message send and receive, mute, block and report, connection and delivery state, and shared entity links that leak no private knowledge. | Nothing exists and nothing is planned. There is no chat surface, no channel, no participant list, and no transport for a message to cross. | Every section rests on there being a second human manager to talk to, and there is not one: [`CONTEXT.md`](../../../CONTEXT.md)'s **Save** entry fixes exactly one human manager per Save, and the multiplayer, network and host axis was removed wholesale from this project at Group A. AI-managed clubs are simulated, not correspondents. Returns only if a Save gains multiple human managers, which would be a new effort against a redrawn scope, not a resumption of this one. |
