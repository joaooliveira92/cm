/**
 * PROTOTYPE — throwaway. See ../README.md.
 *
 * Variant A — "Admiralty chart" — was the winning design out of ticket 06's
 * three-way comparison, so its implementation now lives permanently at
 * `components/world-map/WorldMapChart.tsx` (used by the F1 world map sheet).
 * This file just keeps variant A in the comparison bar for B and C's sake.
 */

export { WorldMapChart as VariantAChart } from "../../../components/world-map/WorldMapChart.js";
