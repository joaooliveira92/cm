/** The two TanStack table ids the Transfers screen owns. */
import type { TableId } from "../table/types.js";

export const MARKET: TableId = "transfer-market";
export const FREE: TableId = "free-agents";
